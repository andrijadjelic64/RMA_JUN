<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="rs.raf.jun.andrija_djelic_rn619">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".application.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RMA_JUN">
        <activity
            android:name=".presentation.view.activities.SplashActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.activities.LoginActivity"/>
        <activity android:name=".presentation.view.activities.MainActivity" />
        <activity android:name=".presentation.view.activities.DiscoverActivity" />
        <activity android:name=".presentation.view.activities.PortfolioActivity" />
        <activity android:name=".presentation.view.activities.ProfileActivity" />
        <activity android:name=".presentation.view.activities.QuoteActivity" />
    </application>

</manifest>